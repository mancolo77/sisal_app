name: sisal_app
description: "GreenArena: Sports Sections Quiz - Learn across sports sections, answer fast, review smarter — fully offline."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # State management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  
  # Local storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.4
  
  # Utilities
  uuid: ^4.5.1
  intl: ^0.19.0
  
  # Fonts
  google_fonts: ^6.2.1
  
  # Navigation
  go_router: ^14.6.2
  
  # Charts (optional for future stats)
  fl_chart: ^0.69.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # Code generation
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/data/
